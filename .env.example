# Cheleban Environment Configuration
# Copy to .env and fill real values LOCALLY only.
# NEVER commit .env or real secrets to Git.

ENVIRONMENT=development
API_BASE_URL=https://opensky-network.org/api
ADSB_API_URL=https://opensky-network.org/api
# ADSB_API_KEY=   # leave empty unless you use a private proxy
